#!/usr/bin/env python

from AlgsSedgewickWayne.EdgeWeightedDigraph as EdgeWeightedDigraph
import sys

def main(prt=sys.stdout):
  In in = new In(args[0])
  G = EdgeWeightedDigraph(a)
  prt.write("{}\n".format(str(G)))

if __name__ == '__main__':
  main()

